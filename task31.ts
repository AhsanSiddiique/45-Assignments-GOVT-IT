let users: string[] = ['Ahsan', 'Madina', 'Amna'];

// first it will check if the usernames aray is empty or filled 
if (users.length > 0) 
// bcz the array is filled we empty the usenames
  { users = [];
    // and here we print a messsege that the usenamees have been removed
    console.log('All usernames have been removed.'); }
    else
    console.log('We need to find some users!');
//and here the msg is print after it declares that the array of users has been empty out  
